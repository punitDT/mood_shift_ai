rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Audio files - readable by anyone with the signed URL
    match /audio/{fileName} {
      allow read: if true;
      allow write: if false; // Only Cloud Functions can write via Admin SDK
    }
  }
}

